{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Read",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}